# 第 4 天 - 队长任务清单

## 今日定位

第 4 天的重点不是继续扩展大量新功能，而是对第 3 天已经合入 `main` 的主流程进行收口验收，确保项目可以稳定演示。

今日主线：

```text
同步远端 -> 检查分支 -> 修正页面细节 -> 跑通主流程 -> 记录测试和问题 -> 安排后续任务
```

## 队长今日任务表

| 优先级 | 任务 | 具体动作 | 完成标准 |
| --- | --- | --- | --- |
| 高 | 同步本地和远端 | 执行 `git fetch`、`git pull --ff-only`，检查 `main` 与 `origin/main` | 本地 `main` 与远端一致，无未合并远端分支 |
| 高 | 检查分支状态 | 查看远端未合并分支和最近 PR 合并结果 | 明确旧 review 分支已合入并被删除 |
| 高 | 页面收口 | 修正学生管理操作入口、评价页提示、作业提交记录页面文案 | 页面入口更符合主流程，无明显误导 |
| 高 | 编译测试 | 执行 `mvn -q test` | Maven 测试通过 |
| 高 | 主流程验收 | 启动本地服务，测试登录、学生、作业、AI 评价、教师复核、统计、导出 | 完整链路可以跑通 |
| 中 | 浏览器渲染检查 | 打开导出页和评价页，确认关键内容可见 | 页面能正常渲染统计摘要、评价步骤和教师复核区域 |
| 中 | 更新测试记录 | 将第 4 天验收步骤和结果写入测试记录 | 老师能看到验收依据 |
| 中 | 更新问题记录 | 记录今日发现和处理的问题 | 后续任务有依据 |
| 中 | 整理日报 | 形成第 4 天日进度汇报表 | 可直接作为队长今日工作材料 |

## 给 B 同学的任务

| 优先级 | 任务 | 具体要求 | 验收标准 |
| --- | --- | --- | --- |
| 高 | 页面文案统一 | 将页面中“作品 / 作业 / 作业列表”统一为当前业务口径 | 页面不出现明显混用或错字 |
| 高 | 导航体验检查 | 检查未审批、AI 已审批、已完成、学生管理、作业提交、导出成绩入口 | 当前页高亮正确，跳转不失效 |
| 中 | 表格和按钮细节 | 检查表格空状态、按钮宽度、操作菜单 | 页面适合演示，不出现遮挡或错位 |

## 给 C 同学的任务

| 优先级 | 任务 | 具体要求 | 验收标准 |
| --- | --- | --- | --- |
| 高 | 统计逻辑复核 | 检查 `aiEvaluatedCount`、`teacherConfirmedCount`、平均分计算 | AI 评价和教师复核后统计数字能更新 |
| 高 | 导出接口推进 | 已将 `/api/export/excel` 从示例地址推进为真实 `.xlsx` 文件流下载 | 导出能力可在本地验证为真实 Excel |
| 中 | 参数校验补强 | 已补充 `StudentRequest`、`SubmissionRequest` 和教师复核校验 | 接口错误提示更清晰 |

## 今日验收结果

| 验收项 | 结果 |
| --- | --- |
| 本地与远端同步 | 通过 |
| 远端未合并分支 | 通过，未发现需继续合并的功能分支 |
| `mvn -q test` | 通过 |
| 页面 HTTP 状态 | `/`、`/students`、`/submit`、`/assignments/pending`、`/assignments/ai-reviewed`、`/assignments/completed`、`/export`、`/swagger-ui/index.html` 均为 200 |
| 登录验证 | 通过，`teacher / 123456` 登录后跳转 `/students` |
| 新增作业 | 通过，生成第 4 天验收作业 |
| AI 评价 | 通过，返回模拟分数、问题和评语 |
| 教师复核 | 通过，保存最终分数和评语 |
| 统计摘要 | 通过，复核后教师已确认数和平均分更新 |
| 导出接口 | 通过，`/api/export/excel` 可返回真实 `.xlsx` 文件 |

## 第 4 天最终补充验收

| 验收项 | 结果 |
| --- | --- |
| C 同学 Excel / DTO 内容 | 已合入 `main` |
| B 同学登录页、侧边栏和状态页 JS | 已合入 `main` |
| 状态页卡“正在加载”问题 | 已修复，`/api/evaluations` 和状态页 JS 均已恢复 |
| Excel 下载 | 已验证返回 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，文件为 Microsoft OOXML |

## 后续建议

| 时间 | 建议 |
| --- | --- |
| 第 5 天 | 继续补充线上部署后的 Excel 下载验证 |
| 第 5 天 | 继续完善 Request DTO 覆盖范围和异常提示 |
| 第 6 天 | 评估真实文件上传，明确文件大小、类型、存储路径 |
| 第 6 天以后 | 评估真实 AI 接入方案，保留 `FakeAIService` 作为演示降级方案 |
