# 2026-05-29 日进度汇报表

## 今日目标

- 开始为前后端分离做接口和页面基线准备。
- 保持旧 Thymeleaf 页面可演示，不破坏现有主流程。
- 合并 C 同学后端接口稳定性调整和 B 同学前端体验优化。
- 明确成功响应兼容规则，避免旧页面 JS 因统一响应包装失效。

## 今日完成内容

### 1. 完成后端 API 基础适配

- 新增 CORS 配置，支持独立前端本地端口访问 `/api/**`。
- 新增统一响应 DTO `ApiResponse`，为后续前后端分离预留统一响应结构。
- 完善 `ApiExceptionHandler`，继续保持错误响应为 `{ "message": "..." }`。
- 补充设计期文档中的项目结构和接口说明。

### 2. 保持接口响应兼容

复查后发现旧 Thymeleaf 页面中的 JS 仍按裸数组或裸对象解析接口响应，例如：

- `/api/students`
- `/api/submissions`
- `/api/evaluations`
- `/api/statistics/summary`

因此调整 `ApiResponseAdvice` 策略：

- 不自动包装所有 `@RestController` 成功响应。
- 只对 Controller 方法显式返回 `ApiResponse` 的场景生效。
- 保持当前旧页面和已有接口调用不被破坏。

### 3. 完成前端页面体验优化

B 同学前端分支合入后，旧页面体验继续完善：

- 状态页新增 Snackbar 通知组件。
- 侧边栏收起态定位和居中效果修正。
- 状态页样式继续优化，保留旧系统可演示能力。

### 4. 完成前后端分离准备文档

新增并整理第 5-6 天合并阶段任务清单：

- `docs/工作清单/d5-d6/README.md`
- `docs/工作清单/d5-d6/第5-6天合并-队长任务清单.md`

文档中明确：

- 前后端分离不一次性拆掉旧系统。
- 后端继续保持 API 路径和字段稳定。
- 前端后续可从学生管理或作业状态页开始迁移。
- 旧 Thymeleaf 页面继续作为演示兜底。

## 今日验收结果

- `main` 已合入 CORS、错误处理、响应兼容说明和前端体验优化。
- 旧页面仍按原接口响应格式运行。
- 成功响应未被统一包装，避免影响现有页面 JS。
- 前后端分离准备文档已补充。

## 遇到的问题

### 1. 统一响应包装与旧页面兼容存在冲突

问题：

- 如果直接把所有成功响应包装成 `{ code, message, data }`，旧页面 JS 会因为期望数组或对象而解析失败。

处理方式：

- 调整 `ApiResponseAdvice`，暂不全局包装所有成功响应。
- 在文档中记录兼容规则。

当前状态：

- 已解决。

### 2. 前后端分离需要保留旧系统兜底

问题：

- 独立前端工程尚未落地，如果直接拆旧页面会影响演示。

处理方式：

- 明确第 5-6 天阶段只做准备和样板迁移，不删除 Thymeleaf 页面。

当前状态：

- 已记录到任务清单。

## 未完成事项

- 独立前端工程尚未正式建立。
- 学生管理页还没有迁移到独立前端。
- 真实文件上传尚未实现。
- 真实 AI 接入尚未实现。
- API 成功响应格式后续如需统一，需要前端请求封装和旧页面同步适配。

## 明日计划

- 继续确认前后端分离启动基线。
- 准备第 7 天任务清单和三人分工。
- 检查 CORS、Swagger、核心 API 和旧页面可演示状态。
- 让 B/C 同学从最新 `main` 新建各自第 7 天功能分支。
