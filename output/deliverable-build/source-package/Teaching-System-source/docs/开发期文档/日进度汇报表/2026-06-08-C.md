# 一些问题记录

## 数据库相关

### 缺失班级表
我们需要name、grade、description

班级表需要和学生表绑定，不如说，学生表需要追加一个classId

### 缺失作业表
这怎么设计？

作业标题、详细介绍、附件列表、受理班级、发布日期（自动填入）、截止日期

作业表需要一个存储班级表中classId的字段，用于作业与班级的绑定

### 修改提交表
提交表怎么修改？需要修改吗？似乎不用？

哦，学生可能不止上传一个文件，但是目前的提交表只支持单文件的上传

或者，我们强制要求学生在有多个文件的时候使用压缩包？这好吗？这不好。

等等，提交表是提交到哪一个作业上的？似乎不知道，提交表需要一个作业表的作业ID

不对，提交表还缺少打回状态，让我想想：

初次提交-AI未评价、初次提交-AI已评价、初次提交-教师已评价
被打回-AI未评价

对的，对的，被打回的话，可以利用已有的status字段。

我们还需要一个作业提交更新时间

### 修改评价表
哦哦哦，真是操他妈的，评价表缺少了很多关键信息

我们需要评价表的AI评价创建时间、AI评价更新时间，教师评价创建时间、教师评价更新时间

评价可能需要跨端保存，所以还需要一个草稿评分和草稿评语

### 缺少认证表
我知道不着急，但是我们就不能创建好吗？然后提供一个H2样例，账号名是teacher，密码是123456

## Python侧

### OCR服务启动即崩溃
AI这么说的：
```python
# ai-service/ScreenshotProc/src/screenshotproc/server.py:30
# 引用了 _load_models(), _det_predictor, _rec_predictor
# 但 ocr.py 从 Surya OCR 重构为 EasyOCR，这些符号不存在
# 服务启动时 ImportError / AttributeError，截图 OCR 功能完全不可用。
```

### 杂项
1. Evaluator 包缺少 __init__.py
2. DocxConv 评估端点 /api/evaluate 返回硬编码分数 82.50（标注为"假"）

## Java侧

### 杂项
1. 缺少 PUT /api/students/{id} 更新接口 - StudentController
2. 缺少 PUT/DELETE /api/submissions/{id} - SubmissionController
3. 缺少 DELETE /api/submissions/{submissionId}/evaluation - EvaluationController
4. 导出接口不支持按作业筛选 - ExportController

以上是AI说的

首先，学生提交了作业后，就没法删除自己的提交了，或许我们需要开放这个功能

教师提交了批改后，也没法删除自己的批改，学生需要看得到AI的评价吗？那自然不应该了，所以只要可以删除教师评价就好了

导出作业要支持：导出全部，导出某班级下所有/指定作业，导出某作业委派的所有/指定班级的作业，导出某个学生的全部/指定作业

## 前端

### 杂项
1. ReviewPage 文件"预览""下载"按钮无 @click 处理器 - ReviewPage.vue
2. 文件大小硬编码 100 KB - ReviewPage.vue
3. "账户设置"按钮无点击处理 - MainLayout.vue
4. fileType 由前端通过扩展名推导，后端 WorkSubmission 无此字段

我的天，是预览大人，我们该怎么实现？

我看到提交表有文件大小字段，现在可以用吗？支持多文件吗？

账户设置都要有什么呢？偏好上传至云端、未提交的修改上传云端、主色调修改、暗色模式状态（始终明亮或者其他）

我看到提交表也有扩展名字段，但是可以用吗？支持多文件吗？