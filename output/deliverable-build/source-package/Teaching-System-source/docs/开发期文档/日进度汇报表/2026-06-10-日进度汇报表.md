# 2026-06-10 日进度汇报表

## 今日目标

- 基于最新 `main` 完成第二天收口任务。
- 复核三端服务状态，完成队长与 C 同学真实 AI 联调。
- 检查 B 同学前端 PR，确认 `/review` 文件预览/下载是否真正进入主线。
- 将通过验收的 PR 合入 `main`。
- 补齐今日测试记录、日进度和复盘文档。

## 今日完成内容

### 1. 三端服务状态复核

当前最终基线：

```text
main: 0f09ac7 feat: add submission file preview and download
```

服务检查结果：

| 服务 | 地址 | 结果 |
| --- | --- | --- |
| Vue 前端 | `http://127.0.0.1:5173` | 可用于最终演示，前端 build 通过 |
| Java 后端 | `http://127.0.0.1:8080/api/health` | 返回 `status=UP` |
| Python AI | `http://127.0.0.1:8000/api/health` | 返回 `status=ok`，DeepSeek、Pandoc、LibreOffice、EasyOCR 可用 |

最终构建检查：

```bash
cd frontend && npm run build
```

结果：通过。

### 2. C 同学真实 AI 联调完成

今日完成队长与 C 同学真实 AI 链路联调。

联调结果：

- AI 服务 `.env` 已在本地配置，文件已被 Git 忽略。
- Pandoc 已安装，版本为 `3.9.0.2`。
- LibreOffice 已安装，版本为 `26.2.4.2`。
- Python AI `/api/health` 通过。
- Python AI `/api/health/deepseek` 通过，模型为 `deepseek-v4-flash`。
- Java `POST /api/submissions/1/evaluate` 可调 Python AI 并返回真实评分。
- `/api/evaluate/stream` SSE 流式评分已通过截图验收。
- 教师复核保存后，Java 可通知 Python 写入最终评价日志。
- `ai-service/.env` 和 `ai-service/logs/` 均未进入 Git。

关键结果：

```text
aiScore = 84.0
dimensionScores = 4 项
teacherConfirmed = true
status = 2
```

说明：跨电脑访问 C 同学 Windows 电脑 `172.22.63.169:8000` 时网络链路不稳定，最终采用队长电脑本地启动 Python AI 的方式完成正式联调。该方式不影响验收结论，因为代码、接口和模型调用链路一致。

### 3. B 同学 PR 审核与合并完成

B 同学提交了两个前端 PR：

- #84 `feat: 文件预览/下载功能 & 路由懒加载安全加固`
- #85 `Feature/d14 2 base`

审核过程：

- PR #83 不是文件预览/下载任务，未合并。
- PR #84 / #85 原始版本包含核心功能，但同时带入了当前主线未接入的旧页面和依赖。
- 原始版本前端构建失败，缺失 `element-plus`、`pinia`、`@/stores/submission`、`@/composables/usePolling` 等依赖或模块。
- 队长在 `feature/d14-2-base` 上追加收口提交 `14c30f2 fix: keep file preview PR buildable`，删除未接入主线的旧页面，保留文件预览/下载核心能力。
- 修复后 `cd frontend && npm run build` 通过。
- PR #85 已 squash merge 到 `main`。

合并信息：

| 项目 | 结果 |
| --- | --- |
| PR | `https://github.com/ShanHuiSir/Teaching-System/pull/85` |
| main 提交 | `0f09ac7 feat: add submission file preview and download` |
| 合并时间 | `2026-06-10 22:34:50 +0800` |

合入内容：

- `ReviewPage.vue` 附件大小从固定 `100 KB` 改为真实 `fileSize` 格式化展示。
- “预览”按钮接入 `doPreviewFile(...)`。
- “下载”按钮接入 `doDownloadFile(...)`。
- 新增 `useFileActions.ts` 管理预览和下载行为。
- 新增 `FloatingPreview.vue` 浮动预览窗口。
- 新增 `FilePreviewPage.vue` 和 `/preview/:submissionId` 独立预览路由。
- 保留路由懒加载。

验收命令：

```bash
rg -n "100 KB|<button class=\"ghost-btn\">预览|<button class=\"ghost-btn\">下载" frontend/src/views/ReviewPage.vue
rg -n "formatFileSize|doPreviewFile|doDownloadFile|FloatingPreview|useFileActions|/preview/:submissionId" frontend/src/views/ReviewPage.vue frontend/src/router/index.ts frontend/src/composables/useFileActions.ts frontend/src/components/FloatingPreview.vue frontend/src/views/FilePreviewPage.vue
curl -D - http://127.0.0.1:8080/api/submissions/1/file -o /tmp/submission-1-file-check.bin
cd frontend && npm run build
```

验收结果：

- 未再检索到旧 `100 KB` 硬编码和静态“预览/下载”按钮。
- `/api/submissions/1/file` 返回 HTTP 200。
- 前端 production build 通过。

## 今日文档更新

更新：

- `docs/开发期文档/测试记录/测试记录.md`

新增：

- `docs/开发期文档/日进度汇报表/2026-06-10-日进度汇报表.md`
- `docs/开发期文档/日进度汇报表/2026-06-10-队伍复盘纪要.md`

## 当前风险与注意事项

### 1. DeepSeek Key 已在聊天中出现过

虽然 `.env` 已被 Git 忽略，Key 没有进入仓库，但该 Key 曾在聊天记录中明文出现。演示结束后建议更换或吊销该 Key。

### 2. B 同学旧页面扩展不能继续混入最终主线

#85 原始版本里带入的 `WorksPage`、`PendingPage`、`CompletedPage` 等页面并未与当前新版工作台主线对齐。后续如果要继续做这些页面，必须单独开 PR，并先补齐依赖、接口和路由口径。

### 3. 演示仍依赖三端同时启动

最终演示需要同时确认：

- 前端 Vite 服务。
- Java 后端服务。
- Python AI 服务。

如果 Python AI 未启动，Java 可能降级或真实评分失败，需要在演示前预热 health 和固定样例。

## 明日收口建议

明日不再新增功能，进入最终演示准备：

1. 固定演示脚本：登录、仪表盘、班级/作业、审批、AI 评分、教师复核、文件预览/下载、导出。
2. 准备固定测试数据和固定文件样例。
3. 演示前复跑三端 health。
4. 演示前复跑 `cd frontend && npm run build`。
5. 确认 `.env`、`ai-service/logs/`、临时下载文件不出现在 `git status`。

## 当前进度判断

第二天收口任务已完成：

- C 同学真实 AI 联调已完成。
- B 同学文件预览/下载 PR 已完成审核、修正并合入 `main`。
- 队长完成三端复核、合并决策和文档补写。

当前项目进入最终演示准备状态。
