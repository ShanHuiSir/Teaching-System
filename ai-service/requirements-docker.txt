# Lightweight Docker dependencies for the demo AI service.
# OCR/EasyOCR is intentionally excluded because it pulls large Torch wheels
# (~2 GB).  This file is a strict subset of requirements.txt — the only
# difference is the absence of "easyocr>=1.7.0".
#
# Install: pip install -r requirements-docker.txt

# HTTP server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0

# DOCX processing
python-docx>=1.1.0
lxml>=5.0.0
pymupdf>=1.25.0

# DeepSeek / LLM (Evaluator)
openai>=1.0.0

# Spreadsheet & Presentation extraction
openpyxl>=3.1.0
python-pptx>=1.0.0

# Image (OCR support — EasyOCR excluded; see note at top)
Pillow>=10.0.0

# Archive processing
py7zr>=0.20.0
rarfile>=4.0
