# 第二天任务清单

## 队长

负责后端、数据库和项目整合。
第 1 步：进入项目目录

打开终端，执行：

cd /Users/niou9/Documents/B1-教学评价系统
确认位置：

pwd
应该显示：

/Users/niou9/Documents/B1-教学评价系统
第 2 步：拉取最新代码

git pull
如果用 GitHub Desktop，就点：

Fetch origin
Pull origin
第 3 步：先确认项目能编译

mvn compile
看到：

BUILD SUCCESS
再继续。

第 4 步：创建 Student.java

新建文件：

src/main/java/com/teachingeval/Student.java
内容先写这个：

package com.teachingeval;

public class Student {
    private Long id;
    private String sno;
    private String name;
    private String major;
    private String className;

    public Student() {
    }

    public Student(Long id, String sno, String name, String major, String className) {
        this.id = id;
        this.sno = sno;
        this.name = name;
        this.major = major;
        this.className = className;
    }

    public Long getId() {
        return id;
    }

    public String getSno() {
        return sno;
    }

    public String getName() {
        return name;
    }

    public String getMajor() {
        return major;
    }

    public String getClassName() {
        return className;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public void setSno(String sno) {
        this.sno = sno;
    }

    public void setName(String name) {
        this.name = name;
    }

    public void setMajor(String major) {
        this.major = major;
    }

    public void setClassName(String className) {
        this.className = className;
    }
}
第 5 步：创建 StudentDAO.java

新建文件：

src/main/java/com/teachingeval/StudentDAO.java
内容先写这个，暂时用内存假数据，后面再接数据库：

package com.teachingeval;

import java.util.ArrayList;
import java.util.List;

public class StudentDAO {
    private final List<Student> students = new ArrayList<>();

    public StudentDAO() {
        students.add(new Student(1L, "2026001", "张三", "软件工程", "软件 1 班"));
        students.add(new Student(2L, "2026002", "李四", "软件工程", "软件 1 班"));
    }

    public List<Student> findAll() {
        return students;
    }

    public void add(Student student) {
        students.add(student);
    }
}
第 6 步：再次编译

mvn compile
必须看到：

BUILD SUCCESS
如果成功，说明你的后端学生实体和 DAO 已经完成第一版。

第 7 步：启动项目检查页面没坏

mvn exec:java -Dexec.mainClass=com.teachingeval.App
浏览器打开：

http://localhost:8080
检查：

teacher / 123456 能登录
/students 能打开
/evaluation 能打开
/export 能打开
第 8 步：提交代码

GitHub Desktop 里会看到两个新增文件：

Student.java
StudentDAO.java
Summary 写：

添加学生实体和DAO
然后点：

Commit to main
Push origin
明天做到这里就合格

你的完成标准：

Student.java 已创建
StudentDAO.java 已创建
mvn compile 成功
原来的登录和页面没有坏
代码已提交并 Push
如果还有时间，再做进阶一步：把 /students 页面里的写死学生，改成从 StudentDAO.findAll() 读取。这个可以后面我继续带你做。

## B 同学

负责前端页面。
B同学任务表：

类型	     要做什么	    具体要求	             验收标准
明日任务	 优化登录页。	账号、密码、登录按钮清楚	页面不乱，能正常登录
明日任务	 优化学生管理页。	表格、按钮、操作入口清楚	能看到学生列表
明日任务	 优化作品评价页。	AI 结果和教师复核区域清楚	页面适合答辩演示
明日任务	 统一页面样式。	  统一颜色、字体、按钮、表格、表单	   整体风格一致
禁止事项	 不引入复杂前端。	不用 Vue、React，不做动画大屏	 不破坏现有流程

## C 同学

负责 AI 模块和文档。

明日任务	创建 AI 结果类。	新建 AIResult.java	包含 score、problems、comment
明日任务	创建假 AI 服务。	新建FakeAIService.java	代码能编译
明日任务	实现评价方法。	evaluate(String studentName, String fileName)	返回固定评分、问题、评语
明日任务	自测编译。	写完后执行 mvn compile	出现 BUILD SUCCESS

禁止事项	不接真实 AI	不申请 API Key，不调外部接口	只做假 AI 模块