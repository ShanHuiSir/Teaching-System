# 第二天任务清单

## 今日目标

第二天不再新建重复的空类，重点是把现有学生管理、AI 评价和页面流程整理成一个能演示的小闭环：

1. 项目能编译、能启动。
2. 教师能用 `teacher / 123456` 登录。
3. `/students` 能新增、查看、删除学生。
4. `/evaluation` 能调用假 AI，显示评分、问题和评语。
5. 三个人各自提交代码，最后由队长统一拉取、编译、运行检查。

## 启动前统一步骤

所有人开始前先做这几步：

```bash
cd /Users/niou9/Documents/B1-教学评价系统
git pull
mvn compile
```

看到 `BUILD SUCCESS` 后再开始改代码。

运行项目用：

```bash
mvn spring-boot:run
```

浏览器打开：

```text
http://localhost:8080
```

## 队长

负责后端、数据库、接口整合和最终验收。

### 任务 1：检查学生管理后端

当前项目已经有这些文件，不要重复新建到 `com.teachingeval` 根包：

- `src/main/java/com/teachingeval/model/Student.java`
- `src/main/java/com/teachingeval/repository/StudentRepository.java`
- `src/main/java/com/teachingeval/service/StudentService.java`
- `src/main/java/com/teachingeval/controller/StudentController.java`

需要确认的接口：

| 接口 | 作用 | 验收标准 |
| --- | --- | --- |
| `GET /api/students` | 查询学生列表 | 页面能加载学生列表 |
| `POST /api/students` | 新增学生 | 输入学号、姓名、班级后能新增 |
| `DELETE /api/students/{id}` | 删除学生 | 点击删除后列表刷新 |

### 任务 2：检查数据库和字段

确认学生表字段和代码一致：

| 字段 | 代码字段 | 说明 |
| --- | --- | --- |
| `id` | `id` | 主键 |
| `student_no` | `studentNo` | 学号，不能重复 |
| `name` | `name` | 姓名 |
| `class_name` | `className` | 班级 |

如果需要补文档，就更新：

- `docs/设计规范/数据库字段设计.md`
- `sql/schema.sql`

### 任务 3：最终整合验收

所有人提交后，队长执行：

```bash
git pull
mvn compile
mvn spring-boot:run
```

浏览器检查：

- `/` 能打开登录页。
- 使用 `teacher / 123456` 能登录。
- `/students` 能新增学生。
- `/students` 能删除学生。
- `/evaluation` 点击 AI 评价后能显示结果。
- `/export` 页面没有报错。
- Swagger 能打开：`http://localhost:8080/swagger-ui/index.html`

### 队长完成标准

- `mvn compile` 成功。
- 学生管理接口和页面能跑通。
- AI 评价接口和页面能跑通。
- 所有人代码已合并，没有明显冲突。
- 项目能用于当天演示。

## B 同学

负责前端页面和样式，主要改：

- `src/main/resources/templates/login.html`
- `src/main/resources/templates/students.html`
- `src/main/resources/templates/evaluation.html`
- `src/main/resources/templates/export.html`
- `src/main/resources/static/style.css`

### 任务清单

| 类型 | 要做什么 | 具体要求 | 验收标准 |
| --- | --- | --- | --- |
| 必做 | 优化登录页 | 标题、账号、密码、登录按钮清楚 | 页面不乱，能正常登录 |
| 必做 | 优化学生管理页 | 表格、新增表单、删除按钮清楚 | 能看到学生列表，能新增和删除 |
| 必做 | 优化作品评价页 | AI 结果区、教师复核区清楚 | 点击 AI 评价后结果显示明显 |
| 必做 | 统一页面样式 | 颜色、字体、按钮、表格、表单风格一致 | 页面看起来像同一个系统 |
| 可选 | 优化导出页 | 加一个成绩导出占位说明和按钮 | 答辩时能解释后续导出功能 |

### 前端注意事项

- 不引入 Vue、React。
- 不做复杂动画和大屏效果。
- 不改接口路径。
- 不把原来能登录、能新增学生、能 AI 评价的流程改坏。
- 修改后至少自己跑一次项目检查页面。

### B 同学完成标准

- 登录页不乱。
- 学生管理页能正常操作。
- 作品评价页适合答辩演示。
- 全站样式统一。
- `mvn compile` 成功。

## C 同学

负责 AI 模块和接口文档，主要看：

- `src/main/java/com/teachingeval/dto/AIEvalRequest.java`
- `src/main/java/com/teachingeval/model/AIEvaluationResult.java`
- `src/main/java/com/teachingeval/service/AIService.java`
- `src/main/java/com/teachingeval/service/FakeAIService.java`
- `src/main/java/com/teachingeval/controller/EvaluationController.java`
- `docs/interface/AI评价模块.md`

### 任务清单

| 类型 | 要做什么 | 具体要求 | 验收标准 |
| --- | --- | --- | --- |
| 必做 | 检查 AI 请求类 | 确认包含 `submissionId`、`studentName`、`fileName` 等页面需要字段 | 前端传 JSON 后后端能接收 |
| 必做 | 检查 AI 结果类 | 确认包含 `aiScore`、`aiIssues`、`aiComment`、`status` | 页面能显示评分、问题、评语 |
| 必做 | 完善假 AI 服务 | `FakeAIService` 返回固定评分、问题和评语 | 点击 AI 评价能看到模拟结果 |
| 必做 | 补 AI 接口文档 | 写清楚请求地址、请求示例、返回示例 | 队友能按文档联调 |
| 可选 | 准备答辩说明 | 说明当前是假 AI，后续可替换真实大模型接口 | 答辩时能讲清楚技术路线 |

### AI 模块注意事项

- 第二天不接真实 AI。
- 不申请 API Key。
- 不调用外部网络接口。
- 不改 `/api/evaluate` 这个接口路径。
- 返回字段要和前端页面里的字段名一致。

### C 同学完成标准

- `/api/evaluate` 能正常返回假 AI 结果。
- Swagger 里能看到 AI 评价接口。
- AI 接口文档写清楚。
- `mvn compile` 成功。

## 今日最终验收清单

当天结束前，三个人一起按下面顺序检查：

1. `git pull`
2. `mvn compile`
3. `mvn spring-boot:run`
4. 浏览器打开 `http://localhost:8080`
5. 登录系统。
6. 新增一名学生。
7. 从学生列表进入评价页。
8. 点击 AI 评价。
9. 检查评分、问题、评语是否显示。
10. 打开导出成绩页。

以上全部通过，第二天任务完成。

## 提交建议

每个人提交时 Summary 可以这样写：

- 队长：`完善学生管理后端和整合检查`
- B 同学：`优化前端页面和统一样式`
- C 同学：`完善AI评价模拟服务和接口文档`

提交前都要先执行：

```bash
mvn compile
```
