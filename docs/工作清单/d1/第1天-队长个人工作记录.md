# 第 1 天 - 队长个人工作记录

## 负责人

队长 / 项目整合与学生管理模块负责人

## 今日完成内容

### 1. 完成项目基础运行检查

- 确认项目可以通过 Maven 编译运行。
- 检查 Spring Boot 项目结构是否正常。
- 确认本地页面可以访问。
- 确认 Swagger / OpenAPI 页面可以正常展示接口。

### 2. 完成学生管理后端接口

新增学生管理相关后端结构：

- `Student.java`
- `StudentRepository.java`
- `StudentService.java`
- `StudentController.java`

完成接口：

- `GET /api/students`：查询学生列表
- `POST /api/students`：新增学生
- `DELETE /api/students/{id}`：删除学生

接口已在 Swagger 中展示，并通过本地测试。

### 3. 完成接口异常处理

新增：

- `ApiExceptionHandler.java`

用于处理接口异常，例如：

- 学号重复
- 学生不存在

让接口返回更清晰的错误信息，方便前端联调和 Swagger 测试。

### 4. 完成学生管理页面前后端联调

将 `students.html` 从静态页面改为可调用后端接口的页面。

已实现：

- 页面自动加载学生列表
- 新增学生
- 删除学生
- 从学生列表进入评价页面

对应接口：

- `GET /api/students`
- `POST /api/students`
- `DELETE /api/students/{id}`

### 5. 完成 AI 评价页面前后端联调

将 `evaluation.html` 接入 AI 评价接口。

已实现：

- 输入学生姓名、作品文件名、作品提交 ID
- 点击“AI 评价”调用 `POST /api/evaluate`
- 页面展示 AI 评分建议
- 页面展示 AI 发现的问题
- 页面展示 AI 评语草稿
- 自动填入教师复核区域

### 6. 完成 GitHub 提交与同步

今日已完成并推送以下提交：

- `新增学生管理接口`
- `完成学生管理和AI评价前后端联调`

当前本地代码与 GitHub `main` 分支保持同步。

## 今日验证结果

- `mvn compile` 编译通过。
- `http://localhost:8080/students` 页面可正常操作。
- `http://localhost:8080/evaluation` 页面可调用 AI 评价接口。
- `http://localhost:8080/swagger-ui/index.html` 可查看接口文档。

## 当前已跑通流程

```text
新增学生
→ 查看学生列表
→ 进入评价页面
→ 调用 AI 评价接口
→ 显示 AI 评分、问题和评语
```

## 今日遗留问题

- 学生数据目前使用 H2 内存数据库，重启后数据会清空。
- AI 评价目前仍使用 `FakeAIService`，还没有接入真实大模型。
- AI 评价结果还没有保存到数据库。
- 教师最终评分和评语还没有保存接口。
- 作品提交模块尚未完成。
- 成绩统计和 Excel 导出尚未完成。
- 线上 `redtree.miprota.cc` 的 Swagger 文档仍未开放。

## 第二天计划

- 完成作品提交模块基础设计。
- 增加作品提交相关接口。
- 推进 AI 评价结果保存。
- 增加教师最终评分保存功能。
- 继续完善成绩统计与导出功能。
