# 测试记录

本文档用于记录关键流程测试结果，重点记录系统是否能按业务流程跑通。

## 记录格式

```markdown
## YYYY-MM-DD

### 测试目标

说明本次测试要验证什么。

### 测试环境

- 本地 / 线上：
- 浏览器：
- 后端端口：

### 测试步骤

1. 登录系统。
2. 新增学生。
3. 提交作品。
4. 执行 AI 评价。
5. 保存教师最终评价。
6. 查看成绩统计。

### 测试结果

- 通过 / 未通过：
- 问题说明：
```

## 2026-05-27

### 测试目标

验证第三天实现阶段的基础版本是否能够启动，并为 B/C 分支合并后的完整主流程联调做准备。

### 测试环境

- 本地 / 线上：本地
- 分支：`feature/d3-base`
- 浏览器：待 B/C 分支合并后补充
- 后端端口：`8080`
- 数据库：H2 内存数据库

### 当前状态

- 基础版本可以启动。
- 当前后端使用 H2 初始化测试数据。
- 当前 AI 评价由 `FakeAIService` 返回模拟结果，尚未接入真实大模型。
- 当前 Excel 导出仍为入口占位，尚未实现真实文件下载。

### 测试数据

- 学生数量：8 个
- 作品数量：6 个
- AI 已评价作品：3 个
- 教师已确认作品：1 个

### 待测内容

1. B/C 分支合并后的完整主流程。
2. 登录后是否可以进入学生管理页。
3. 学生列表是否能从 `/api/students` 正常加载。
4. 新增学生是否可以保存。
5. 作品是否可以提交到 `/api/submissions`。
6. 作品列表进入评价页时是否携带 `submissionId`。
7. 评价页是否可以调用 `/api/submissions/{submissionId}/evaluate`。
8. AI 评价结果是否可以显示分数、问题和评语。
9. 教师最终分数和评语是否可以保存到 `/api/submissions/{submissionId}/teacher-review`。
10. 导出成绩页是否可以调用 `/api/statistics/summary` 展示统计摘要。

### 测试结果

- 通过 / 未通过：待 B/C 分支合并后补充
- 问题说明：待 B/C 分支合并后补充
