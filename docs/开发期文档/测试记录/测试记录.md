# 测试记录

本文档用于记录关键流程测试结果，重点记录系统是否能按业务流程跑通。

## 记录格式

```markdown
## YYYY-MM-DD

### 测试目标

说明本次测试要验证什么。

### 测试环境

- 本地 / 线上：
- 浏览器：
- 后端端口：

### 测试步骤

1. 登录系统。
2. 新增学生。
3. 提交作品。
4. 执行 AI 评价。
5. 保存教师最终评价。
6. 查看成绩统计。

### 测试结果

- 通过 / 未通过：
- 问题说明：
```

## 2026-05-27

### 测试目标

验证第三天实现阶段的主流程整合结果，确认 B/C 分支合并后系统关键页面和接口可以正常工作。

### 测试环境

- 本地 / 线上：本地
- 分支：`main`
- 浏览器：Codex 内置浏览器
- 后端端口：`8081`（8080 被旧 Java 进程占用，临时使用 8081 验收）
- 数据库：H2 内存数据库

### 当前状态

- `main` 分支已合入 B/C 当天功能和 PR #40 冲突解决结果。
- 当前后端使用 H2 初始化测试数据。
- 当前 AI 评价由 `FakeAIService` 返回模拟结果，尚未接入真实大模型。
- 当前 Excel 导出仍为入口占位，尚未实现真实文件下载。
- 注：以上为 2026-05-27 验收时状态；2026-05-28 最终合并后，Excel 导出已更新为真实 `.xlsx` 文件下载。

### 测试数据

- 学生数量：8 个
- 作品数量：6 个
- AI 已评价作品：3 个
- 教师已确认作品：1 个

### 测试步骤

1. 拉取远端最新 `main` 分支。
2. 执行 `mvn test` 检查项目是否可编译。
3. 临时使用 8081 启动项目。
4. 打开 `/export`，检查统计摘要是否正常展示。
5. 打开 `/evaluation?submissionId=1&studentId=1&studentName=张三&fileName=student-work.zip`。
6. 点击“执行 AI 评价”，检查 AI 分数、问题和评语是否显示。
7. 填写教师最终分数和评语，点击“保存最终评价”。
8. 检查页面是否提示教师复核已保存。

### 测试结果

- 通过 / 未通过：通过
- 编译结果：`mvn test` 执行成功，结果为 `BUILD SUCCESS`。
- 统计页结果：学生总数 8，作品提交数 6，AI 已评价 3，教师已确认 1，教师平均分 92。
- 评价页结果：可以执行 AI 模拟评价，可以显示 AI 分数、问题和评语，可以保存教师最终分数和评语。
- 问题说明：8080 被旧 Java 进程占用，本次验收临时使用 8081；真实 AI、真实文件上传、真实 Excel 导出仍为后续任务。

## 2026-05-28

### 测试目标

验证第 4 天队长收口结果，确认本地 `main` 与远端 `origin/main` 同步后，系统完整主流程可以继续稳定运行。

### 测试环境

- 本地 / 线上：本地
- 分支：`main`
- 后端端口：`8081`
- 数据库：H2 内存数据库
- 浏览器：Codex 内置浏览器

### 测试步骤

1. 执行 `git fetch --all --prune` 同步远端引用。
2. 执行 `git pull --ff-only` 将本地 `main` 快进到最新 `origin/main`。
3. 执行 `mvn -q test` 检查项目测试和编译状态。
4. 使用 `8081` 启动本地服务。
5. 检查页面 HTTP 状态：`/`、`/students`、`/submit`、`/assignments/pending`、`/assignments/ai-reviewed`、`/assignments/completed`、`/export`、`/swagger-ui/index.html`。
6. 使用 `teacher / 123456` 登录，确认跳转到 `/students`。
7. 调用 `/api/submissions` 新增一条第 4 天验收作业。
8. 调用 `/api/submissions/{submissionId}/evaluate` 执行 AI 模拟评价。
9. 调用 `/api/submissions/{submissionId}/teacher-review` 保存教师最终评价。
10. 调用 `/api/statistics/summary` 检查统计摘要是否更新。
11. 调用 `/api/evaluations` 检查状态页批量评价结果接口。
12. 调用 `/api/export/excel` 检查 Excel 文件下载响应。
13. 用浏览器打开 `/export`、`/assignments/ai-reviewed` 和 `/evaluation`，检查统计摘要、状态列表和教师复核区域是否可见。

### 测试结果

- 通过 / 未通过：通过
- 本地与远端状态：`main` 与 `origin/main` 一致。
- 远端分支状态：未发现需要继续合并的远端功能分支。
- Maven 测试：`mvn -q test` 通过。
- 页面访问：主要页面和 Swagger 均返回 200。
- 登录结果：`teacher / 123456` 登录后返回 302，并跳转 `/students`。
- 主流程结果：新增作业、AI 评价、教师复核均成功。
- 统计结果：新增验收作业并完成复核后，统计摘要更新为学生 8、作业 7、AI 已评价 4、教师已确认 2、教师平均分 90。
- 状态页接口：`/api/evaluations` 返回 200，可用于未审批、AI 已审批、已完成页面匹配评价状态。
- 导出结果：`/api/export/excel` 返回 200，响应类型为 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，下载文件为 Microsoft OOXML `.xlsx`。
- 浏览器渲染：导出页可见统计摘要和导出按钮，AI 已审批页不再卡“正在加载”，评价页可见作业评价、学生与作业信息、教师复核区域。

### 问题说明

- 当前 AI 评价仍使用 `FakeAIService`，尚未接入真实大模型。
- 当前作业提交保存作业信息和文件名，尚未实现真实文件上传。
- 当前真实 Excel 导出已在本地通过，线上部署后仍需复测下载。

### 状态列表自动刷新回归测试

#### 测试背景

教师在使用状态页时发现：

- 新增作业提交后，`/assignments/pending` 的“未审批”列表不会立即显示新增作业，需要手动刷新页面。
- 在评价页执行 AI 评价后，`/assignments/pending` 与 `/assignments/ai-reviewed` 的状态迁移不会立即体现，需要手动刷新页面。
- 教师保存最终复核后，`/assignments/ai-reviewed` 与 `/assignments/completed` 的状态迁移不会立即体现，需要手动刷新页面。

#### 修复范围

本次修复集中在前端页面刷新机制，不修改数据库结构和后端主流程。

- `src/main/resources/static/js/assignment-page.js`
  - 状态列表页请求 `/api/students`、`/api/submissions`、`/api/submissions/{id}/evaluation` 时增加缓存规避参数。
  - 增加列表自动同步机制，页面可见时每 3 秒重新拉取一次数据。
  - 增加 `focus`、`pageshow`、`visibilitychange` 监听，用户切回页面或返回页面时自动刷新。
  - 增加请求序号控制，避免旧的异步请求结果覆盖新的页面数据。
  - 在状态页内提交作业成功后，写入 `localStorage` 变更标记并立即刷新当前列表。
  - 在 AI 已审批页或已完成页提交新作业后，自动跳转到未审批页并强制重新渲染。
- `src/main/resources/templates/evaluation.html`
  - AI 评价完成后写入作业状态变更标记。
  - 教师复核保存成功后写入作业状态变更标记。
  - AI 评价完成后自动跳转到 AI 已审批页并强制重新渲染。
  - 教师复核保存成功后自动跳转到已完成页并强制重新渲染。
- `src/main/resources/templates/submit.html`
  - 独立作业提交页保存成功后写入作业状态变更标记。
  - 独立作业提交页保存成功后自动跳转到未审批页并强制重新渲染。
- `src/main/resources/templates/pending.html`
- `src/main/resources/templates/ai-reviewed.html`
- `src/main/resources/templates/completed.html`
  - 为 `assignment-page.js` 增加版本号参数，避免浏览器继续使用旧脚本缓存。

#### 测试环境

- 本地 / 线上：本地
- 后端端口：`8080`
- 数据库：H2 内存数据库
- 浏览器：Codex 内置浏览器
- 启动方式：`mvn spring-boot:run`
- 编译检查：`mvn test`

#### 初始数据

服务重启后使用 `DataInitializer` 初始化数据：

| 状态 | 初始数量 | 说明 |
| --- | ---: | --- |
| 未审批 | 3 | 提交记录 1、2、3 尚未 AI 评价 |
| AI 已审批 | 2 | 提交记录 4、5 已完成 AI 评价，待教师复核 |
| 已完成 | 1 | 提交记录 6 已完成教师复核 |

#### 测试步骤与结果

| 步骤 | 操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- |
| 1 | 重启服务后打开 `/assignments/pending` | 页面加载新版 `assignment-page.js?v=20260528-2` | 通过，页面脚本地址正确 |
| 2 | 查看未审批列表初始数量 | 显示 `当前有 3 份作业待审批` | 通过，显示 3 条 |
| 3 | 在 `/submit` 通过页面表单新增作业提交：赵六，文件名 `redirect-check.zip` | 保存成功后自动进入未审批页，未审批列表显示新作业 | 通过，页面跳转到 `/assignments/pending?...`，显示 `当前有 4 份作业待审批`，表格出现 `redirect-check.zip` |
| 4 | 在未审批页点击新作业“审批”，进入评价页后点击“执行 AI 评价” | AI 评价成功后自动进入 AI 已审批页，新作业从未审批移出 | 通过，页面跳转到 `/assignments/ai-reviewed?...`，AI 已审批显示 `当前有 3 份作业待教师复核`，表格出现 `redirect-check.zip` |
| 5 | 在 AI 已审批页点击新作业“复核” | 评价页能加载 AI 评价结果和教师复核区域 | 通过，能看到教师复核输入框和保存按钮 |
| 6 | 填写教师最终分数 `89` 和评语，点击“保存最终评价” | 保存成功后自动进入已完成页，新作业从 AI 已审批移出 | 通过，页面跳转到 `/assignments/completed?...`，已完成显示 `当前有 2 份作业已完成` |
| 7 | 查看已完成列表 | 已完成列表显示新作业和最终分数 | 通过，列表出现 `redirect-check.zip`，最终分数为 `89` |
| 8 | 执行 `mvn test` | 项目编译和测试通过 | 通过，结果为 `BUILD SUCCESS`，当前项目无测试源码，显示 `No tests to run` |

#### 接口验证记录

新增作业提交响应：

```json
{
  "id": 7,
  "studentId": 4,
  "studentName": "赵六",
  "title": "页面跳转验证作业",
  "fileName": "redirect-check.zip",
  "workType": "代码压缩包",
  "remark": "验证列表自动刷新"
}
```

AI 评价响应摘要：

```json
{
  "submissionId": 7,
  "aiScore": 82.50,
  "status": 1,
  "aiEvaluated": true
}
```

教师复核响应摘要：

```json
{
  "submissionId": 7,
  "teacherScore": 89.00,
  "teacherComment": "页面跳转验证通过。",
  "status": 2
}
```

#### 测试结论

- 通过 / 未通过：通过
- 未审批、AI 已审批、已完成三个状态页已经可以自动同步数据。
- 新增提交、AI 评价、教师复核三个状态变更节点均可触发页面数据更新。
- 页面不再依赖用户手动刷新才能看到状态变化；关键操作成功后会直接跳转到对应状态页。
- 注意事项：如果服务已经在改动前启动，需要重新执行 `mvn spring-boot:run` 或重启 Spring Boot，否则运行中的服务可能仍使用旧的 `target/classes` 资源。
