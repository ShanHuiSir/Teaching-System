# 问题与解决记录

本文档用于记录影响项目推进的问题和处理过程。

## 记录格式

```markdown
## YYYY-MM-DD

### 问题

描述遇到的问题。

### 影响

说明影响哪些页面、接口、部署或演示流程。

### 原因分析

说明初步判断的原因。

### 解决办法

说明如何处理。

### 当前状态

未解决 / 已解决 / 暂时绕过。
```

## 2026-05-27

### 问题一：启动时报旧实体类冲突

### 问题

运行项目时出现 `com.teachingeval.entity.Student` 和 `com.teachingeval.model.Student` 实体名重复的问题。

### 影响

项目无法正常启动，影响当天基础版本验收。

### 原因分析

源码中已经以 `entity/Student.java` 为准，但旧编译产物中仍残留 `model/Student.class`，导致 Hibernate 扫描到两个实体名都为 `Student` 的类。

### 解决办法

执行：

```bash
mvn clean spring-boot:run
```

清理 `target/` 旧编译产物后重新启动。

### 当前状态

已解决。

## 2026-05-28

### 问题：main 与 feature/d4-base 短暂分叉

### 问题

第 4 天晚间检查时发现 `main` 与 `feature/d4-base` 曾短暂分叉：

- `main` 已合入 B 同学的登录页、侧边栏、退出逻辑和状态页 JS。
- `feature/d4-base` 保留 C 同学的 Excel 导出、DTO 和参数校验内容。

如果只看 `main`，会误以为 C 同学的真实 Excel 导出和 DTO 内容被覆盖。

### 影响

- `main` 一度缺少真实 Excel 导出、`StudentRequest`、`SubmissionRequest` 等内容。
- 状态页合并后曾出现卡在“正在加载”的风险。
- 复盘时容易把“分支未合回”误判成“代码被覆盖”。

### 原因分析

B 同学分支先合入 `main`，C 同学内容留在 `feature/d4-base`。后续虽然 `feature/d4-base` 又合入 B 同学分支，但还需要再合回 `main` 才算最终进入主线。

### 解决办法

- 从最新 `main` 创建修复分支。
- 将 `feature/d4-base` 内容合入该分支。
- 恢复状态页依赖的 `/api/evaluations` 批量评价接口。
- 恢复 `EvaluationService.listEvaluations()`。
- 修复 `assignment-page.js` 中状态页加载依赖的 `lastReloadAt` 和 `loadSeq`。
- 通过 PR 将修复分支合入 `main`。

### 当前状态

已解决。当前 `main` 已包含：

- C 同学真实 Excel 导出、DTO、参数校验。
- B 同学登录页、侧边栏和状态页 JS。
- 状态页加载修复。

## 2026-05-28

### 问题：第 4 天早期文档中 Excel 状态滞后

### 问题

第 4 天早期日报、测试记录和任务清单中写到 Excel 导出仍为示例地址或占位能力。

### 影响

后续复盘时可能误以为真实 Excel 导出仍未完成。

### 原因分析

这些文档写在 C 同学 `feature/d4-base` 最终合入 `main` 之前。当时 `/api/export/excel` 确实仍是示例地址。

### 解决办法

更新第 4 天相关文档，将最终状态修正为：

- `/api/export/excel` 已返回真实 `.xlsx` 文件流。
- Excel 导出已在本地验证通过。
- 线上部署后仍需复测。

### 当前状态

已解决。

## 2026-05-28

### 问题：学生列表一次性查询存在 OOM 风险

### 问题

GitHub Issue #17 反馈：`StudentService.listStudents()` 直接调用 `studentRepository.findAll()`，当学生数量逐渐增多时，会一次性把全部学生加载到内存中，存在导致 OOM 的风险。

### 影响

- 学生管理页访问 `/api/students` 时可能一次性返回大量数据。
- 远程调试或真实教学班级数据变大后，后端内存压力和前端渲染压力都会增加。
- 后续如果导入大量学生，旧接口容易成为性能瓶颈。

### 原因分析

当前学生列表接口最初为小规模演示数据设计，默认只有 8 个学生，因此直接使用 `findAll()`。但该实现没有分页边界，随着数据增长不适合继续作为主查询方式。

### 解决办法

- 新增分页查询接口：

```text
GET /api/students/page?page=0&size=20
```

- `StudentService` 新增分页方法，使用 `PageRequest` 和 Spring Data JPA 分页查询。
- 对分页参数做保护：
  - `page` 小于 0 时按 0 处理。
  - `size` 最小为 1。
  - `size` 最大限制为 200。
- 保留旧接口 `GET /api/students` 作为兼容入口，但最多返回前 200 条学生记录，避免无限制 `findAll()`。
- 学生管理页改为调用分页接口，默认每页 20 条，并增加上一页、下一页和总数提示。

### 验证结果

- `mvn test` 执行通过。
- 学生管理页不再通过 `/api/students` 一次性加载全部学生，而是请求 `/api/students/page?page=0&size=20`。
- 旧接口仍可用于当前作业提交、评价等下拉框兼容场景，但已限制最多返回 200 条。

### 当前状态

已解决。

## 2026-05-28

### 问题：开发环境缺少一键重置演示数据能力

### 问题

GitHub Issue #34 反馈：当前系统没有销毁 / 重置数据按钮，数据保存在 H2 内存数据库中，远程调试或多人联调时，如果学生、作业、评价数据被删除或改乱，难以快速恢复到初始化测试状态。

### 影响

- 学生管理页删除学生后，需要重启服务或手动操作 H2 才能恢复测试数据。
- 远程演示时数据状态不可控，容易影响主流程验收。
- 未审批、AI 已审批、已完成三个状态页的演示数据可能被前一次测试污染。

### 原因分析

项目当前使用 `DataInitializer` 在应用启动时灌入初始化数据，但只在启动阶段自动执行；运行过程中没有对外提供重置入口。H2 又是内存数据库，适合开发演示，但缺少页面按钮会让恢复数据不够直观。

### 解决办法

- 将 `DataInitializer` 的初始化逻辑拆出为可复用方法。
- 新增开发环境接口：

```text
POST /api/dev/reset-demo-data
```

- 接口执行时依次清空评价、作业、学生数据，再重新写入初始化学生、作业和评价结果。
- 使用 `@Profile("!prod")` 限制该接口只在非生产环境启用。
- 在学生管理页新增“重置数据”按钮，点击后弹出确认提示，确认后调用重置接口并重新加载学生列表。
- 重置成功后写入作业状态变更标记，通知状态页同步刷新。

### 验证结果

- 删除 1 个学生后，`/api/students` 返回数量从 8 变为 7。
- 调用 `POST /api/dev/reset-demo-data` 返回：

```json
{"message":"演示数据已重置"}
```

- 再次检查：
  - `/api/students` 返回 8 个学生。
  - `/api/submissions` 返回 6 条作业。
  - `/api/statistics/summary` 返回学生 8、作业 6、AI 已评价 3、教师已确认 1、教师平均分 92。
- `mvn test` 执行通过。

### 当前状态

已解决。

## 2026-05-27

### 问题二：PR #39 与 main 存在 sidebar.html 冲突

### 问题

PR #39 合并时提示以下文件冲突：

```text
src/main/resources/templates/fragments/sidebar.html
```

### 影响

PR #39 无法直接合并到 `main`，影响 B 同学作业状态入口拆分功能进入主分支。

### 原因分析

`main` 中已经调整过侧边栏导航，PR #39 也修改了同一段导航结构，导致 Git 无法自动判断应该保留哪些入口。

### 解决办法

- 基于 `origin/main` 创建 `codex/resolve-pr-39` 分支。
- 本地合并 PR #39 并手动解决冲突。
- 保留 PR #39 中“未审批 / AI 已审批 / 已完成”三个状态入口。
- 保留 `main` 中已有的“学生管理 / 作业提交 / 导出成绩”入口。
- 运行 `mvn test` 验证通过。
- 通过 PR #40 合并到 `main`。

### 当前状态

已解决。

## 2026-05-27

### 问题三：8080 端口被旧 Java 进程占用

### 问题

验收时运行 `mvn spring-boot:run`，提示 `Port 8080 was already in use`。

### 影响

项目无法使用默认端口 8080 启动，影响接口和页面验收。

### 原因分析

本机已有旧 Java 进程监听 8080 端口。

### 解决办法

- 临时使用 8081 启动项目进行验收。
- 验收结束后关闭临时 8081 服务。
- 后续如需使用 8080，需要先关闭旧 Java 进程。

### 当前状态

暂时绕过。

## 2026-05-28

### 问题一：本地 main 落后远端 main

### 问题

第 4 天检查时发现本地 `main` 落后 `origin/main` 4 个提交。

### 影响

如果直接在旧本地代码上验收，可能遗漏远端已经合入的文档和分支处理结果。

### 原因分析

远端在检查过程中合入了旧 review 分支，`origin/main` 已推进到最新提交。

### 解决办法

执行：

```bash
git pull --ff-only
```

将本地 `main` 快进到最新远端版本。

### 当前状态

已解决。

## 2026-05-28

### 问题二：学生管理页操作入口容易误导

### 问题

学生管理页操作菜单中原有“评价”入口只携带学生信息，未携带具体 `submissionId`，进入评价页后无法直接评价具体作业。

### 影响

主流程演示时容易从学生管理页直接进入空评价页，造成流程断点。

### 原因分析

评价页以作业提交为评价对象，需要从具体作业记录进入，而不是只从学生进入。

### 解决办法

将学生管理页操作入口从“评价”改为“作业”，并跳转到：

```text
/submit?studentId={id}
```

让教师先为学生提交或选择作业，再从作业状态页进入评价。

### 当前状态

已解决。

## 2026-05-28

### 问题三：部分页面文案仍保留“作业列表”

### 问题

评价页空状态和作业相关页面仍使用“作业列表”表述，与当前侧边栏拆分后的“未审批 / AI 已审批 / 已完成 / 作业提交”入口不完全一致。

### 影响

影响演示时对页面入口的解释，容易让老师误以为仍有一个独立的作业列表入口。

### 原因分析

第 3 天合并作业状态入口拆分后，部分提示文案未同步更新。

### 解决办法

- 将 `works.html` 页面标题调整为“作业提交记录”。
- 将评价页空状态提示调整为从“未审批 / AI 已审批 / 作业提交记录”进入。
- 将 JS 错误提示统一为“作业提交记录加载失败”。

### 当前状态

已解决。
