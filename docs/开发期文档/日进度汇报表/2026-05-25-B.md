# 2026-05-26 日进度汇报表

## 今日目标

1. 根据指导老师建议，明确文档维护方式。
2. 将文档拆分为设计期文档和开发期文档。
3. 确定每日进度汇报表的存放位置和格式。
4. 统一全站前端 UI/UX，以 MD3 设计体系为统一基础，按 B 端应用风格调整间距与信息密度。

## 今日完成内容

### 文档体系搭建

- 新增 `docs/README.md`，作为项目文档入口。
- 新增 `docs/设计期文档/` 目录，用于保存设计期文档。
- 新增 `docs/开发期文档/日进度汇报表/` 目录，用于保存每日汇报。
- 明确详细接口文档后续以 Swagger / OpenAPI 为主。

### 前端 UI/UX 全面重构

- **style.css 完全重写**（168 行 → 695 行）：基于 Material Design 3 规范构建 B 端设计令牌体系，覆盖 Color / Elevation / Shape / Motion / Typography / Spacing 六大维度，实现 15 个统一组件模块（md-topbar、md-card、md-btn、md-field、md-table、md-chip、md-toolbar、md-step、md-score、md-info-grid、md-dialog、md-empty 等）。
- **登录页优化**：移除 HTML 中硬编码的账号密码，采用 MD3 表单组件，优化错误提示卡片样式。
- **学生管理页优化**：新增搜索工具栏、MD3 表格 hover 交互、chip 标签、tonal 操作按钮，页面头部操作栏分离。
- **作品评价页重设计**：三步工作流布局（①学生信息 → ②AI 评价结果 → ③教师复核），56px 大号评分数字，问题/评语双栏卡片，适合答辩演示。
- **导出成绩页统一**：操作按钮移至页面头部，空状态组件引导。
- **退出确认对话框**：全局导航栏退出按钮 hover 红色高亮 + 确认对话框（ESC / 遮罩关闭）。
- 新增 `docs/UI-UX-工作报告-20260526.md`，详细记录 UI/UX 改动细节与设计决策。

## 如何完成

1. 先检查原有 docs 目录结构，保留原有工作清单和早期文档。
2. 新增更清晰的 `设计期文档/` 和 `开发期文档/` 目录。
3. 用 Markdown 编写文档入口、设计期目录结构说明。
4. 分析原有前端代码，诊断问题（无 JS、硬编码数据、样式零散、导航重复）。
5. 确定以 MD3 设计令牌体系为统一基础，逐页面重构 HTML 模板，统一组件类名。
6. 每次修改后执行 `mvn compile` 验证编译通过，启动应用 curl 验证全部页面 HTTP 200。

## 遇到的问题

1. 原有文档分散在 `工作清单/`、`设计规范/`、`interface/` 等多个目录，缺少统一入口和发展阶段区分。
2. 部分接口文档和数据库设计文档属于早期版本，与当前代码状态不完全同步。
3. 应用启动时偶遇端口 8080 被占用，需手动 kill 旧进程后重启（此问题仅在吴承睿的电脑上出现）。

## 解决办法

1. 用 `docs/README.md` 作为文档总入口，清晰标注各目录用途。
2. 每天新增一份日进度汇报表，保留历史记录。
3. 设计期文档和开发期文档分目录维护，前者放设计，后者放进度与复盘。
4. 接口详细字段不重复手写，交给 Swagger 维护（后端已配置 SpringDoc OpenAPI）。

## 未完成事项

1. 设计期文档仍需补充：项目结构说明、数据库设计概要、接口设计总体方针、组件交互设计、命名规范等。
2. HTML 原型页面中按钮（新增学生、AI 评价、保存评价、导出 Excel）仍无 JS 事件处理，需等后端 API 就绪后对接。
3. 导航栏尚未提取为 Thymeleaf fragment（`th:fragment`），目前 3 个页面各自维护。
4. 学生管理页表格数据仍为硬编码，需等 Student CRUD API 完成后改为 Thymeleaf 动态渲染。

## 明日计划

1. 完成《设计期文档系列》剩余文档（项目结构、数据库设计、接口总体方针、组件交互、命名规范）。
2. 将右上侧导航交互移至左侧状态栏，做成可收纳式侧边栏，仅保留"新增学生"和"退出"两个主要交互入口。
3. 引导 B 同学制作 HTML 原型页面（按页面清单创建）。
4. 引导 C 同学根据页面补充业务流程和说明文档。
