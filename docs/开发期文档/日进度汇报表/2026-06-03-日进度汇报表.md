# 2026-06-03 日进度汇报表

## 今日目标

- 完成第 10 天队长收口任务。
- 拉齐最新 `main`，确认本地代码与 `origin/main` 同步。
- 复测后端自动化测试、前端构建和 Py 服务语法检查。
- 启动 Py FastAPI 服务，验证 `/api/health` 和 `/api/preprocess`。
- 开启 Java 侧 `app.preprocess.enabled=true`，复测真实文件上传和 Java-Py 预处理链路。
- 按演示主流程跑通上传、AI 评价降级、教师复核、统计和 Excel 导出。
- 补齐第 10 天测试记录、日报和风险说明。

## 今日完成内容

### 1. 拉齐最新 main

执行：

```bash
git status --short --branch
git pull --ff-only
```

结果：

- 本地 `main` 原本落后 `origin/main` 4 个提交。
- 已通过 fast-forward 同步到最新远端。
- 同步后的改动集中在 Py 服务配置和启动脚本。

### 2. 完成三端基线验收

后端测试：

```bash
mvn -q test
```

结果：

- 通过。
- 覆盖学生分页、重复学号错误、真实文件上传、Java 调 Py Mock 预处理、真实 AI Mock 调用、评价、复核、统计和 Excel 导出等场景。

前端构建：

```bash
npm --prefix frontend run build
```

结果：

- 构建通过。
- 构建过程中存在 Rollup 注释提示和大 chunk 提示，不影响当前验收。
- 构建生成的临时产物已清理，避免混入源码变更。

Py 语法检查：

```bash
python3 -m py_compile ai-service/DocxConv/src/docxconv/server.py
```

结果：

- 通过。

### 3. 完成 Py 服务健康检查和预处理验收

检查 Py 服务：

```bash
curl -sS http://localhost:8000/api/health
```

返回：

```json
{
  "status": "unavailable",
  "libreofficeAvailable": false
}
```

说明：

- Py FastAPI 服务可访问。
- 当前机器未安装 LibreOffice，DOCX 版式渲染不可用，但不影响文本提取主流程。
- 路径口径：本地 Py 服务直接访问 `/api/health`；线上 `redtree.miprota.cc` 经过网关前缀，Swagger 中 AI 服务路径为 `/ai/api/health`。

真实 DOCX 预处理测试：

```bash
curl -sS -F "file=@docs/基于大模型的软件实训评价系统-简要需求与时间安排.docx" \
  http://localhost:8000/api/preprocess
```

结果摘要：

```json
{
  "fileType": "docx",
  "textLength": 1656,
  "renderStatus": "failed",
  "renderEngine": "libreoffice",
  "hasStructuredContent": true
}
```

文本文件预处理测试：

```json
{
  "fileType": "text",
  "textLength": 3481,
  "renderStatus": "none",
  "renderEngine": "none"
}
```

结论：

- `/api/preprocess` 能正常返回 `extractedText`、`warnings`、`renderStatus`、`renderEngine`、`renderWarnings`。
- DOCX 在 LibreOffice 缺失时返回 warning，但结构化文本提取成功。
- 本地调试使用 `http://localhost:8000/api/preprocess`，线上 Swagger 的 AI 服务使用 `/ai/api/preprocess`。

### 4. 完成 Java-Py 真实上传预处理联调

Java 验收实例启动在 `8081`，避免影响本机已有 `8080` 服务：

```bash
mvn -q spring-boot:run \
  -Dspring-boot.run.arguments="--server.port=8081 --app.preprocess.enabled=true --app.ai.real.enabled=false"
```

基础接口检查：

- `GET /api/health` 返回 `UP`。
- `GET /api/students/page?page=0&size=3` 返回学生分页数据。
- `GET /api/statistics/summary` 返回初始化统计摘要。

真实 DOCX 上传：

```bash
POST /api/submissions/upload
```

参数：

- `studentId=1`
- `title=第十天联调验收DOCX`
- `workType=实验报告`
- `remark=队长第十天验收上传`
- 文件：`docs/基于大模型的软件实训评价系统-简要需求与时间安排.docx`

结果摘要：

```json
{
  "id": 7,
  "studentName": "张三",
  "fileSize": 39186,
  "preprocessStatus": "SUCCESS",
  "preprocessMessage": "Py预处理完成",
  "preprocessTextLength": 1656,
  "renderStatus": "failed",
  "renderEngine": "libreoffice"
}
```

结论：

- Java 后端成功保存真实上传文件。
- Java 成功调用 Py `/api/preprocess`。
- Java 成功保存 Py 返回的预处理结果。
- LibreOffice 缺失只作为 warning 记录，不阻断上传和预处理主流程。

### 5. 完成演示主流程排练

演示链路：

```text
教师登录 -> 学生管理 -> 上传真实作品 -> Py 预处理 -> AI 评价降级 -> 教师复核 -> 统计摘要 -> Excel 导出
```

接口验收结果：

- AI 评价：`POST /api/submissions/7/evaluate` 返回 AI 模拟评价结果，`status=1`。
- 教师复核：`POST /api/submissions/7/teacher-review` 保存 `teacherScore=88.50`，`status=2`。
- 统计摘要：复核后返回学生 8、作业 7、AI 已评价 4、教师已确认 2、教师平均分 90.25。
- Excel 导出：`POST /api/export/excel` 返回 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，文件识别为 Microsoft OOXML。

页面验收结果：

- 登录页可正常打开，使用 `teacher / 123456` 可进入系统。
- `/students`、`/submit`、`/evaluation`、`/assignments/pending`、`/assignments/ai-reviewed`、`/assignments/completed`、`/export` 均可正常打开。
- 侧边栏状态徽标能反映新增和复核后的作业状态。

### 6. 补齐第 10 天验收文档

已更新：

- `docs/开发期文档/日进度汇报表/2026-06-03-日进度汇报表.md`
- `docs/开发期文档/测试记录/测试记录.md`
- `docs/开发期文档/问题与解决记录/问题与解决记录.md`

## 今日验收结果

| 验收项 | 结果 | 说明 |
| --- | --- | --- |
| `main` 同步 | 通过 | 已 fast-forward 到 `origin/main` |
| 后端测试 | 通过 | `mvn -q test` 通过 |
| 前端构建 | 通过 | `npm --prefix frontend run build` 通过 |
| Py 语法检查 | 通过 | `py_compile` 通过 |
| Py health | 通过 | 服务可访问，LibreOffice 不可用 |
| Py preprocess | 通过 | DOCX 文本提取成功，渲染失败 warning 可见 |
| Java-Py 上传预处理 | 通过 | `preprocessStatus=SUCCESS` |
| AI 评价 | 通过 | 默认 FakeAI 降级可用 |
| 教师复核 | 通过 | 最终评分和评语保存成功 |
| 统计摘要 | 通过 | 统计数据更新正确 |
| Excel 导出 | 通过 | 返回真实 `.xlsx` 文件 |
| 页面演示入口 | 通过 | 登录和核心页面可打开 |

## 遇到的问题

### 1. 当前机器未安装 LibreOffice

影响：

- DOCX 版式渲染返回 `renderStatus=failed`。
- 页面截图、分页、字体、表格位置等版式检查无法在当前机器验收。

处理方式：

- 继续使用 DOCX 结构化文本提取作为主流程。
- 将 LibreOffice 缺失作为 warning 展示和文档风险说明。

当前状态：

- 暂时绕过，不阻塞 MVP。

### 2. 真实 DeepSeek API Key 未验证

影响：

- `/api/evaluate/real` 的线上真实评分效果尚未最终验收。

处理方式：

- Java 默认保留 `FakeAIService` 降级。
- 自动化测试已覆盖 Java 调 Py `/api/evaluate/real` 的 Mock 场景。
- 有可用 Key 后再开启 `app.ai.real.enabled=true` 做真实评分复测。

当前状态：

- 暂时绕过，不阻塞演示。

### 3. Py 启动脚本执行方式需说明

现象：

- 直接执行 `./start.sh` 时当前环境提示 `permission denied`。
- 使用 `bash start.sh` 或 `python -m docxconv serve --host 0.0.0.0 --port 8000` 可以启动。

处理方式：

- 第 10 天验收时使用端口上已有 Py 服务完成检查。
- 后续可补充脚本执行权限或在文档中统一写明启动方式。

当前状态：

- 暂时绕过，不阻塞验收。

## 未完成事项

- 尚未用真实 DeepSeek API Key 验证线上真实评分效果。
- 当前机器未安装 LibreOffice，DOCX 版式渲染未验收。
- OCR 中文识别质量和速度仍作为后续优化项。
- 前端大 chunk 可在后续通过拆包优化，不影响当前功能。
- 固定教师账号仍是演示方案，完整权限系统不在当前 MVP 范围内。

## 当前进度判断

当前项目已完成教师端 MVP 的主要闭环：

```text
登录 -> 学生管理 -> 真实文件上传 -> Py 预处理 -> AI 评价/降级 -> 教师复核 -> 统计 -> Excel 导出
```

整体进度约为 90%。

如果按 MVP 验收口径，当前已经可以进入演示和验收；如果继续做真实 AI Key、LibreOffice 版式渲染、OCR 中文质量、前端拆包和完整权限，预计还需要 2 到 3 天增强。
