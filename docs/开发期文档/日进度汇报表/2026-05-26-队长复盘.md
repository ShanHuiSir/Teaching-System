# 2026-05-26 队长复盘

## 今日目标

根据指导老师反馈，将项目推进重点从“继续实现功能”调整为“先梳理业务、补齐文档、规划 HTML 原型”，让老师和队友能清楚理解系统的业务流程和后续开发路径。

## 今日完成内容

### 1. 明确文档维护方式

- 确定项目文档统一存放在 `docs/` 目录。
- 将文档分为设计期文档和开发期文档。
- 明确每日进度汇报表使用 Markdown 格式，并存放在 `docs/开发期文档/日进度汇报表/`。
- 明确详细接口说明以后主要依赖 Swagger / OpenAPI，Markdown 文档只维护总体方针和重要约定。

### 2. 完成设计期文档系列整理

已整理并合并设计期文档系列，包括：

- 项目概述与需求分析
- 业务流程设计
- 项目结构
- 数据库设计
- 接口设计总体方针
- 页面设计
- 组件交互设计
- 文件命名规范
- 标识符命名规范
- 数据流说明
- 原型页面清单
- 角色与权限说明
- 阶段计划与范围边界

### 3. 完成今日个人负责内容

完成 `01-项目概述与需求分析.md`，内容包括：

- 项目名称
- 项目背景
- 项目目标
- 使用对象
- 需求分析
- 功能需求
- 非功能需求
- 技术栈

### 4. 规范分支和 PR 流程

- 明确后续尽量不直接写入 `main`。
- 文档、原型、后端功能分别使用独立分支。
- PR 合并前先检查文件类型和修改范围，避免一个 PR 混合日进度、设计文档、代码和问题记录。
- 对队友 PR 进行检查，发现部分 PR 内容混杂，决定不直接合并。

### 5. 梳理队友任务和验收方式

已明确 B、C 同学任务：

- B 同学：负责 HTML 原型页面。
- C 同学：负责设计期文档补充和校对。
- 队长：负责验收原型、文档、Issue 和 PR。

同时整理了验收表，明确从页面齐全、跳转、风格、文档对应、数据流、命名规范、PR 范围等方面进行检查。

## 今日重要决策

### 1. 接口文档以 Swagger 为准

详细接口路径、请求参数、返回值和示例以后主要由 Swagger / OpenAPI 维护，避免 Markdown 文档和代码不同步。

### 2. DTO 命名规范需要统一

初步确定：

- 接口请求对象使用 `Request` 后缀。
- 接口响应对象使用 `Response` 后缀。
- 数据库实体不加 `DTO` 后缀。
- `DTO` 只用于既不是请求也不是响应的内部跨层传输对象。

### 3. Issue 和问题记录分工

- GitHub Issues 用于跟踪日常问题、规范问题和待办任务。
- `问题与解决记录.md` 只记录影响项目推进、汇报或答辩的重要问题。

## 遇到的问题

### 1. HTML 原型暂未进入仓库

检查本地仓库后发现，目前没有 `prototype/` 目录。现有 HTML 页面属于 Spring Boot Thymeleaf 页面，不能直接作为静态 HTML 原型双击打开。

处理方式：

- 明确 B 同学需要提交独立的 `prototype/` 目录。
- 原型页面应包含登录、首页、学生管理、作品提交、AI 评价、成绩统计和导出页面。

### 2. 队友 PR 内容混杂

队友 PR 中同时包含日进度、项目概述、问题记录和任务清单，部分内容会覆盖 main 中已有完整文档。

处理方式：

- 不直接合并该 PR。
- 单独整理第 2 天任务清单为独立 PR。
- 建议队友后续日进度 PR 只修改自己的日进度文件。

### 3. 命名规范仍需补充

当前标识符命名规范中已有 Java 类名、方法名、数据库字段、接口路径等规则，但 Request / Response / DTO 的边界还需要继续补充。

## B 同学当前状态

已安排 B 同学负责 HTML 原型，但本地仓库目前还没有看到 `prototype/` 目录。

后续需要检查：

- 是否提交 `prototype/` 静态页面。
- 页面之间是否能跳转。
- 页面风格是否统一。
- HTML id 是否使用小驼峰命名。
- Issue #14 是否处理。

## C 同学当前状态

设计期文档系列已经基本齐全。

后续需要检查：

- 文档是否和 B 的最终原型页面一致。
- 是否补充 Request / Response / DTO 命名规范。
- 是否明确真实 AI 和 Excel 导出属于后续实现。
- 是否避免把未完成能力写成已完成。

## 明日计划

1. 验收 B 同学的 HTML 原型。
2. 验收 C 同学的设计期文档。
3. 检查 GitHub Issues #13、#14 的负责人和处理进度。
4. 审核队友 PR，确保每个 PR 只做一类事情。
5. 根据原型和文档，准备下一阶段功能实现清单。

## 今日结论

今天的主要任务已经完成：项目文档结构更加清晰，设计期文档系列已经成型，分支和 PR 流程也开始规范。当前最大缺口是 HTML 静态原型尚未提交到仓库，明天应重点验收原型和文档是否对应。
