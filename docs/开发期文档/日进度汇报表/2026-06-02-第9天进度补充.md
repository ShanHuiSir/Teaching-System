# 2026-06-02 第 9 天进度补充

## 今日目标

- 建立第 9 天三端真实联调任务基线。
- 补齐 Py 侧 `/api/preprocess` 渲染状态字段。
- Java 侧新增可配置真实 AI 调用能力，并保留模拟评价降级。
- Vue 前端补齐上传和预处理状态展示。
- 完成后端测试和前端构建验收。

## 今日完成内容

### 1. 新增第 9 天任务清单

新增：

- `docs/工作清单/d9/README.md`
- `docs/工作清单/d9/第9天-三端真实联调任务.md`

任务清单明确了队长、B 同学、C 同学三条线：

- 队长负责 Java-Py 预处理联调、真实 AI 调用方案、Mock 自动化测试和文档记录。
- B 同学负责 Vue 构建验收、上传结果展示、预处理状态展示和评价页失败提示。
- C 同学负责统一 AI 服务入口、补齐预处理渲染状态字段和真实评分接口稳定性。

### 2. 补齐 Py 侧预处理状态字段

修改：

- `ai-service/DocxConv/src/docxconv/server.py`

完成内容：

- `PreprocessResponse` 新增：

```json
{
  "renderStatus": "ok / degraded / failed / none",
  "renderEngine": "libreoffice / word / onlyoffice / none",
  "renderWarnings": []
}
```

- 非 DOCX 文件默认返回 `renderStatus=none`、`renderEngine=none`。
- DOCX 文件会尝试使用 LibreOffice 渲染。
- LibreOffice 渲染成功时返回 `renderStatus=degraded`，提示复杂排版可能与 Word 存在差异。
- LibreOffice 渲染失败时返回 `renderStatus=failed`，但不阻断结构化文本提取。
- `/api/evaluate/real` 继续带出预处理中的渲染状态字段。
- 将 `studentName` 改为 multipart form 字段，方便 Java 真实 AI 客户端调用。

### 3. 新增 Java 真实 AI 调用能力

新增：

- `src/main/java/com/teachingeval/service/impl/RealAIService.java`
- `src/main/java/com/teachingeval/service/impl/DelegatingAIService.java`

修改：

- `AIService`
- `EvaluationService`
- `FakeAIService`
- `application.properties`

完成内容：

- 新增配置：

```properties
app.ai.real.enabled=false
app.ai.real.endpoint-url=http://localhost:8000/api/evaluate/real
app.ai.real.connect-timeout-ms=3000
app.ai.real.read-timeout-ms=30000
```

- 默认仍使用 `FakeAIService`，保证离线演示和原主流程稳定。
- 开启 `app.ai.real.enabled=true` 后，Java 会把真实文件以 multipart 方式发送给 Py `/api/evaluate/real`。
- Py 返回的 `aiScore`、`aiIssues`、`aiComment`、`status` 会映射为 Java `EvaluationResult` 并落库。
- 真实 AI 调用失败时自动降级为模拟评价，避免主流程崩溃。

### 4. 补充真实 AI Mock 自动化测试

修改：

- `src/test/java/com/teachingeval/TeachingSystemFlowTest.java`

新增测试：

- `evaluateSubmissionCanCallRealAiServiceWhenEnabled`

验证内容：

- 开启 `app.ai.real.enabled=true`。
- 使用 Mock HTTP 服务模拟 Py `/api/evaluate/real`。
- Java 评价接口能够把真实文件、学生姓名发送给 Mock Py 服务。
- Mock Py 返回的 AI 分数、问题和评语能够保存为 `EvaluationResult`。

### 5. 补齐 Vue 前端上传和预处理状态展示

修改：

- `frontend/src/types/index.ts`
- `frontend/src/views/SubmitAssignment.vue`
- `frontend/src/views/EvaluationPage.vue`

完成内容：

- `WorkSubmission` 类型补齐：

```text
filePath
fileSize
contentType
preprocessStatus
preprocessMessage
preprocessResult
```

- 作业提交成功后展示：

```text
文件名
文件大小
保存路径
预处理状态
预处理说明
renderWarnings / warnings
```

- 评价页展示预处理状态和 warning。
- AI 评价失败时显示明确错误信息，不再只有弹窗提示。

## 今日验收结果

### 后端测试

执行：

```bash
mvn -q test
```

结果：

- 通过。
- 已覆盖真实 AI Mock 联调新增用例。

### Py 侧语法检查

执行：

```bash
python3 -m py_compile ai-service/DocxConv/src/docxconv/server.py
```

结果：

- 通过。

### 前端依赖安装和构建

执行：

```bash
npm --prefix frontend install
npm --prefix frontend run build
```

结果：

- 依赖安装完成。
- Vue 前端构建通过。
- 构建时存在 chunk 体积提示和 Rollup 注释提示，不影响当前验收。
- `npm install` 提示 2 个 moderate severity vulnerabilities，未执行 `npm audit fix --force`，避免强制升级破坏依赖。

## 未完成事项

- 尚未实际启动 Py FastAPI 服务进行真实 Java-Py 端到端联调。
- 尚未使用真实 DeepSeek API Key 验证 `/api/evaluate/real` 的线上评分效果。
- `ai-service/app.py` 仍是旧 Flask 模拟服务，后续仍需统一或清理启动入口。
- 前端大 chunk 体积后续可考虑拆包优化，但不影响当前功能验收。
- npm 依赖审计提示需要后续评估，不建议当前阶段直接强制修复。

## 明日计划

- 启动 Py FastAPI 服务，开启 Java `app.preprocess.enabled=true` 做真实上传预处理联调。
- 开启 `app.ai.real.enabled=true`，在有 API Key 的环境下验证真实 AI 评分。
- 清理或改造顶层 `ai-service/app.py`，避免误启动旧 Flask 服务。
- 补真实联调测试记录和问题记录。
