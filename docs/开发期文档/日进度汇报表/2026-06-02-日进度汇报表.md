# 2026-06-02 日进度汇报表

## 今日目标

- 检查项目目录和前 8 天任务记录，确认当前真实完成度。
- 对照后端、前端、AI 服务三条线，梳理第 9 天后续开发重点。
- 校准入口文档中已经过期的项目状态描述。
- 明确真实文件上传、Py 预处理和真实 AI 评价之间的未联调环节。

## 今日完成内容

### 1. 完成项目目录全量检查

检查范围：

```text
README.md
docs/工作清单/
docs/开发期文档/日进度汇报表/
docs/开发期文档/测试记录/
docs/开发期文档/问题与解决记录/
src/main/java/com/teachingeval/
src/main/resources/
frontend/
ai-service/
```

检查结论：

- 当前项目已经不是单纯 Thymeleaf 演示版本，仓库中已经包含独立 `frontend/` Vue 工程。
- 后端已经完成学生管理、作品提交、真实文件上传、Py 预处理转发能力、AI 模拟评价、教师复核、统计和 Excel 导出。
- `ai-service/DocxConv` 中已经存在 FastAPI 版 `/api/preprocess` 和 `/api/evaluate/real`。
- 顶层 `ai-service/app.py` 仍是旧 Flask 模拟评价服务，容易造成启动入口混淆。

### 2. 复查后端当前状态

后端主线已完成：

- 学生分页接口和学生管理页面分页。
- 演示数据重置能力。
- 自动化测试 `TeachingSystemFlowTest`。
- 真实 Excel 导出。
- 真实文件上传接口：

```text
POST /api/submissions/upload
```

- 上传后保存文件路径、大小、Content-Type 等元数据。
- `PreprocessClient` 可配置调用 Py 侧 `/api/preprocess`。
- Py 服务未启用或调用失败时，不阻断上传主流程。

今日执行：

```bash
mvn -q test
```

结果：

- 通过。
- 覆盖学生分页、重复学号错误、主评价流程、Excel 导出、真实文件上传、不支持文件类型拦截、Java 转发 Py Mock 服务等场景。

### 3. 复查前端当前状态

`frontend/` 当前已经包含：

- Vite + Vue 3 + TypeScript 工程。
- 基础路由。
- 主布局和侧边栏。
- Axios 请求封装。
- 学生管理页。
- 作业提交页。
- 未审批、AI 已审批、已完成状态页。
- 评价页。
- 导出页。

作业提交页已经调用：

```text
POST /api/submissions/upload
```

当前检查到的问题：

- 本机尚未安装 `frontend/` 依赖，执行 `npm --prefix frontend run build` 时提示 `vue-tsc: command not found`。
- `frontend/src/types/index.ts` 中 `WorkSubmission` 类型尚未补齐上传和预处理字段。
- 前端页面对 `preprocessStatus`、`preprocessMessage`、`preprocessResult`、`renderWarnings` 等状态展示还不完整。

### 4. 复查 AI 服务当前状态

`ai-service` 当前状态：

- `DocxConv` 已有 FastAPI 服务入口。
- `/api/preprocess` 已支持文本、DOCX、PDF、Excel、PPT、图片 OCR、压缩包等输入的文本提取。
- `/api/evaluate/real` 已实现上传文件、预处理、调用 DeepSeek 评分、返回 AI 结果的流程。
- `Evaluator` 包已用于真实评分逻辑。
- `start.sh` 和 `start.bat` 已指向 `python -m docxconv serve --host 0.0.0.0 --port 8000`。

当前未完成：

- `/api/preprocess` 还没有接入第 8 天要求的 `renderStatus`、`renderEngine`、`renderWarnings` 字段。
- 顶层 `ai-service/app.py` 仍保留旧 Flask 模拟服务，需要后续改为说明文件、删除或转发到 FastAPI 入口。
- Java 后端评价主流程在今日早些时候仍通过 `FakeAIService` 返回模拟评价；第 9 天进度补充中已新增可配置真实 AI 调用能力，默认仍保留模拟降级。

### 5. 明确第 9 天主线任务

第 9 天建议目标：

```text
打通真实文件上传 -> Py 预处理 -> 真实 AI 评分 -> Java 落库 -> Vue 展示状态的完整链路。
```

队长任务：

- 更新入口文档，校准当前完成度。
- 组织 Java-Py 真实联调，开启 `app.preprocess.enabled=true` 验证真实 `/api/preprocess`。
- 设计 Java 真实 AI 调用服务，保留 `FakeAIService` 作为离线演示降级。
- 明确 `/api/evaluate/real` 与 Java `EvaluationResult` 的字段映射。

B 同学任务：

- 安装前端依赖并执行 `npm run build` 验收。
- 补齐 `WorkSubmission` 类型中的文件上传和预处理字段。
- 在上传页、作业详情或评价页展示预处理状态、失败原因和 warning。
- 完善 AI 评价中、评价成功、评价失败三类前端状态。

C 同学任务：

- 统一 AI 服务启动入口，避免误启动旧 Flask `app.py`。
- 给 `/api/preprocess` 补齐 `renderStatus`、`renderEngine`、`renderWarnings`。
- 确认 `/api/evaluate/real` 请求方式、字段和返回格式能被 Java 后端稳定调用。
- 提供简单 DOCX、复杂 DOCX、失败场景三类真实联调样例。

## 今日验收结果

- 项目目录和前 8 天任务记录已复查。
- 后端 `mvn -q test` 已通过。
- 已确认前端工程存在，且已开始对接真实上传接口。
- 已确认 Py 侧 FastAPI 服务存在 `/api/preprocess` 和 `/api/evaluate/real`。
- 已确认当前最大缺口是三端真实联调，而不是继续新增分散功能。

## 遇到的问题

### 1. README 中前端状态滞后

问题：

- README 仍写着“独立前端工程尚未落地”。

影响：

- 队友或老师阅读入口文档时，可能误以为前端工程还没有开始。

处理方式：

- 今日同步更新 README，将当前状态修正为“Vue 独立前端工程已建立，仍需安装依赖、构建验收和继续联调”。

当前状态：

- 已处理。

### 2. AI 服务启动入口存在混淆

问题：

- `ai-service/app.py` 是旧 Flask 模拟评价服务。
- 实际第 8 天后主要服务入口在 `DocxConv` FastAPI 服务中。

影响：

- 队友可能误启动旧服务，导致 Java 调 `/api/preprocess` 或 Swagger 合并时找不到真实接口。

处理方式：

- 今日先在日报中记录风险。
- 后续建议将 `app.py` 改为启动说明、兼容转发入口，或移除旧模拟服务。

当前状态：

- 未解决，列入第 9 天任务。

### 3. Java 评价主流程仍未接真实 AI

问题：

- Java `EvaluationService` 当前注入的仍是 `FakeAIService`。
- Py 侧虽然已有 `/api/evaluate/real`，但 Java 尚未调用。

影响：

- 演示主流程里的 AI 评价结果仍是固定模拟数据，不能体现真实大模型评分。

处理方式：

- 第 9 天设计并实现 Java 真实 AI 调用服务。
- 保留模拟服务作为 Py 未启动或无 API Key 时的降级方案。

当前状态：

- 未解决，列入第 9 天 P0 任务。

### 4. 前端依赖未安装，构建尚未验收

问题：

- 执行 `npm --prefix frontend run build` 时提示 `vue-tsc: command not found`。

原因：

- 当前本地 `frontend/` 目录下没有安装 npm 依赖。

处理方式：

- 后续先执行 `npm --prefix frontend install`，再运行 `npm --prefix frontend run build`。

当前状态：

- 暂未处理，列入第 9 天前端验收任务。

## 未完成事项

- Java 尚未真实调用 Py 侧 `/api/evaluate/real`。
- Java-Py `/api/preprocess` 尚未开启真实配置做端到端联调。
- Py 侧 `renderStatus`、`renderEngine`、`renderWarnings` 已在第 9 天进度补充中接入 `/api/preprocess`，后续仍需真实服务联调验收。
- 顶层 `ai-service/app.py` 仍需统一或清理。
- Vue 前端依赖安装和构建验收未完成。
- Vue 页面还需展示预处理状态和 AI 评价失败状态。

## 明日计划

- 新增或完善第 9 天任务清单。
- 启动 Py FastAPI 服务，开启 Java `app.preprocess.enabled=true` 做真实上传预处理联调。
- 推进 Java 真实 AI 调用服务，实现 `/api/evaluate/real` 结果落库。
- 前端安装依赖、执行构建，并补齐上传和预处理状态展示。
- 将 Py 的 `renderStatus`、`renderEngine`、`renderWarnings` 接入统一预处理返回。
