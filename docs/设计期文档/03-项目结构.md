# 03-项目结构

## 仓库顶层

```
Teaching-System/
├── src/                     # 源代码
│   └── main/
│       ├── java/com/teachingeval/   # Java 后端
│       └── resources/               # 静态资源和模板
├── frontend/                # Vue 独立前端工程
├── ai-service/              # Python AI 评价服务
│   ├── start.sh             # Linux/macOS 启动脚本
│   ├── start.bat            # Windows 启动脚本
│   ├── config.py            # AI 服务配置入口
│   ├── app.py               # 旧 Flask 模拟评价服务，后续需统一或清理
│   ├── DocxConv/            # FastAPI 统一预处理与真实评分服务
│   ├── Evaluator/           # DeepSeek / LLM 评分逻辑
│   ├── ScreenshotProc/      # 截图 OCR → 文本 + bbox 坐标
│   ├── ArchiveProc/         # 压缩包解压 + 文件分类
│   └── RoadMap.md           # AI 服务开发路线图
├── sql/                     # 数据库建库脚本
├── docs/                    # 项目文档
├── .github/workflows/       # CI/CD（GitHub Actions）
├── pom.xml                  # Maven 构建配置
├── README.md                # 项目说明
└── CLAUDE.md                # AI 编码规范
```

## 后端 Java 包结构

```
com.teachingeval
├── TeachingSystemApplication.java   # Spring Boot 入口
├── config/
│   ├── ApiResponseAdvice.java       # 统一响应体自动包装
│   ├── CorsConfig.java              # 跨域配置
│   ├── DataInitializer.java         # 演示数据初始化
│   └── OpenApiConfig.java           # Swagger / OpenAPI 配置
├── controller/
│   ├── ApiExceptionHandler.java     # 全局异常处理
│   ├── AiDocsProxyController.java   # Py 侧 OpenAPI 文档代理
│   ├── DevDataController.java       # 开发数据工具
│   ├── EvaluationController.java    # AI 评价 + 教师评审
│   ├── ExportController.java        # 成绩导出
│   ├── HealthController.java        # 健康检查
│   ├── PageController.java          # 页面路由
│   ├── StatisticsController.java    # 成绩统计
│   ├── StudentController.java       # 学生管理
│   └── SubmissionController.java    # 作品提交管理
├── entity/
│   ├── EvaluationResult.java        # 评价结果实体（evaluation 表）
│   ├── Student.java                 # 学生实体（student 表）
│   └── WorkSubmission.java          # 作品提交实体（submission 表）
├── dto/
│   ├── AIEvalRequest.java              # AI 评价请求体
│   ├── ApiResponse.java               # 统一 API 响应体
│   ├── ExportRow.java                  # 成绩导出 Excel 列定义
│   ├── StudentRequest.java             # 学生创建/更新请求体
│   ├── StudentPageResponse.java        # 学生分页查询响应体
│   ├── StatisticsSummaryResponse.java  # 成绩统计响应体
│   ├── SubmissionRequest.java          # 作品提交请求体
│   └── TeacherReviewRequest.java       # 教师评审请求体
├── repository/
│   ├── EvaluationRepository.java    # 评价结果数据访问
│   ├── StudentRepository.java       # 学生数据访问
│   └── SubmissionRepository.java    # 作品提交数据访问
├── service/
│   ├── AIService.java               # AI 评价服务接口
│   ├── EvaluationService.java       # 评价业务逻辑
│   ├── ExportService.java           # Excel 导出业务逻辑
│   ├── PreprocessClient.java        # Java 调 Py 预处理服务客户端
│   ├── PreprocessResult.java        # Py 预处理结果封装
│   ├── StatisticsService.java       # 统计业务逻辑
│   ├── StudentService.java          # 学生管理业务逻辑
│   ├── SubmissionService.java       # 作品提交业务逻辑
│   └── impl/
│       └── FakeAIService.java       # AI 评价模拟实现
```

### 分层约定

| 层      | 包              | 职责                         |
|--------|----------------|----------------------------|
| 控制器    | `controller`   | 接收 HTTP 请求，参数校验，调用 Service |
| 服务接口   | `service`      | 定义业务能力抽象                   |
| 服务实现   | `service.impl` | 业务逻辑实现                     |
| 实体     | `entity`       | JPA 实体，对应数据库表              |
| 数据传输对象 | `dto`          | 请求体 / 响应体，不绑定数据库           |
| 数据访问   | `repository`   | Spring Data JPA 接口         |

## 前端资源

```
src/main/resources/
├── application.properties    # Spring Boot 配置
├── static/
│   └── style.css             # 全局样式（Material Design 3）
└── templates/
    ├── login.html            # 登录页
    ├── students.html         # 学生管理页
    ├── submit.html           # 作品提交页
    ├── works.html            # 作品提交记录页
    ├── pending.html          # 未审批作业页
    ├── ai-reviewed.html      # AI 已审批作业页
    ├── completed.html        # 已完成作业页
    ├── evaluation.html       # 作品评价页（AI 评价 + 教师评审）
    └── export.html           # 成绩导出页
```

当前保留两套前端入口：

- `src/main/resources/templates`：Thymeleaf 演示兜底页面，保证旧主流程仍可访问。
- `frontend/`：第 7 天后启动的 Vue 独立前端工程，用于后续前后端分离联调。

## 独立前端工程

```
frontend/
├── package.json              # npm 脚本和依赖
├── vite.config.ts            # Vite 配置，代理 /api 到 Java 后端
├── index.html
└── src/
    ├── main.ts               # Vue 入口
    ├── App.vue
    ├── router/               # 前端路由
    ├── layouts/              # 主布局
    ├── views/                # 学生、提交、状态、评价、导出页面
    ├── components/           # 通用组件
    ├── styles/               # 全局样式和变量
    ├── types/                # TypeScript 类型定义
    └── utils/                # 请求封装
```

当前状态：

- Vue 工程已建立，作业提交页已开始调用 `POST /api/submissions/upload`。
- 前端仍需安装依赖并执行构建验收。
- 上传和评价页面已补充 Py 预处理状态、warning 和真实 AI 失败状态展示，后续仍需真实服务联调验收。

## AI 服务结构

```
ai-service/
├── start.sh                  # 启动 FastAPI 服务
├── start.bat                 # Windows 启动 FastAPI 服务
├── requirements.txt          # 统一依赖
├── config.py                 # 配置和环境变量读取
├── app.py                    # 旧 Flask 模拟服务，后续需统一或清理
├── DocxConv/                 # FastAPI 主服务，包含 /api/preprocess 和 /api/evaluate/real
├── Evaluator/                # DeepSeek 评分逻辑
├── ScreenshotProc/           # OCR 子工具
├── ArchiveProc/              # 压缩包处理子工具
└── RoadMap.md
```

当前状态：

- Py 侧统一预处理和真实评分接口已经存在。
- Java 后端上传后可配置调用 `/api/preprocess`，默认关闭。
- Java 评价主流程已新增可配置真实 AI 调用能力，默认仍使用 `FakeAIService` 作为离线演示和失败降级。
- `/api/preprocess` 已补齐 `renderStatus`、`renderEngine`、`renderWarnings`，后续仍需启动 Py 服务做真实联调验收。

## 数据库

```
sql/
└── schema.sql    # MySQL 建库脚本（student / submission / evaluation 三张表）
```

当前开发期使用 H2 内存数据库，schema.sql 为正式环境的 MySQL 脚本。

## 文档目录

```
docs/
├── README.md                     # 文档导航入口
├── 设计期文档/
│   ├── 00-设计期文档目录.md        # 设计期文档索引
│   ├── 01-项目结构.md             # 本文档
│   ├── 02-业务流程设计.md
│   ├── 05-页面设计.md
│   └── ...
├── 设计规范/
│   └── 数据库字段设计.md
├── 接口文档/（interface）
│   └── AI评价模块.md
├── 开发期文档/
│   ├── 日进度汇报表/
│   ├── 数据库变更/
│   ├── 测试记录/
│   └── 问题与解决记录/
└── 工作清单/
    └── d1/
```
